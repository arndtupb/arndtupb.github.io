# arndtupb.github.io

This site is now authored with Quarto.

## Edit homepage content

1. Update `index.qmd`.
2. Render HTML with:

   ```bash
   quarto render
   ```

3. Commit both `index.qmd` and the generated `index.html`.

This avoids manually editing `index.html`.
